采用时基列表可以实现（此时由于已经将对应的任务从就绪列表中移除了，故可以不进行优先级表的相应位置为0的操作的，此时仍然是可以切换到这个优先级的其他的任务的；但是要注意，如果该优先级表中的相应位对应的就绪位表只有一个任务的话，则此时是一定要将优先级表的相应位置为0的，不置为0的话此时还是有可能会切换到这个优先级的一个任务的，由于该优先级对应的没有任务，故是不允许进行切换的，故此时优先级表的相应位应该置为0）：一个优先级有多个任务。
采用阻塞延时，由于是采用将优先级表中的相应位置为0的操作：此时该优先级即使有多个任务，也是不会进行调度的，因此是只适用于有一个优先级只有一个任务的情况。