1、
system("stty -icanon");//关闭缓冲区，输入字符无需按回车键直接接受
c=getchar();
当这两个搭配使用时输入字符时就不需要输入回车了，但是所输入的字符会默认显示出来。
2、linux中的接收字符的函数c=getchar( )只有在当前只执行这1个线程时才会去执行，当当前同时执行多个线程时是无法完成c=getchar( )输入的任务的，因为执行c=getchar( )函数时会进行等待（可以利用互斥机制使得当前只执行这一个函数）。